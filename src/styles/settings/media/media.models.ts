/**
 * INTERFACES
 * ================================================
 */
export interface iBreakpoints {
  smallest: string;
  smaller: string;
  small: string;
  medium: string;
  large: string;
  larger: string;
  largest: string;
}

/**
 * INTERFACES
 * ================================================
 */
export enum eBreakpoint {
  SMALLEST = 'smallest',
  SMALLER = 'smaller',
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
  LARGER = 'larger',
  LARGEST = 'largest',
}